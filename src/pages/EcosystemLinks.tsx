import SidebarPage from '../components/SidebarPage'

export default function EcosystemLinks() {
  return (
    <SidebarPage
      eyebrow="Linkages"
      title="Linkages"
      description="Confirmed and suggested relationship links will be managed here so users can reuse strong connections across contexts."
    />
  )
}
